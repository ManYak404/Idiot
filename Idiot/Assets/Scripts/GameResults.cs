using UnityEngine;

public static class GameResults
{
    public static bool playerWon = false; // Default to false, will be set when the game ends
    public static bool botWon = false; // Default to false, will be set when the game ends
}